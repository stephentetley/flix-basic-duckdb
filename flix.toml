[package]
name        = "flix-basic-duckdb"
description = "A database interface for Flix to DuckDB."
version     = "0.3.1"
flix        = "0.69.3"
license     = "Apache-2.0"
authors     = ["Stephen Tetley <stephen.tetley@gmail.com>"]

[dependencies]
"github:stephentetley/flix-pretty" = { version = "0.10.2", security = "unrestricted" }

[mvn-dependencies]
"org.duckdb:duckdb_jdbc" = "1.5.0.0"

